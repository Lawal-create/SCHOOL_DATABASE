pipeline {
    agent any

    stages {
        stage('Build') {
            steps {

                bat 'npm install'
            }
        }
        stage('Deploy') {
            steps {

                sh 'node .jenkins/workspace/SCHOOL_DATABASE/index.js'
            }
        }
    }
}